apiVersion: apps/v1 # API version, v1 is stable for deployments
kind: Deployment    # To allow creation of pods
metadata:
  name: acmeair-booking-byuser-deployment
spec:
  replicas: 1 # Number of pods to run
  selector:
    matchLabels:
      app: acmeair
      tier: booking-byuser
  template:
    metadata:
      annotations:
        vpa.k8s.io/in-place-update-policy: "true"
      labels:
        app: acmeair
        tier: booking-byuser
    spec:
      containers:
        - name: acmeair-booking-byuser-container
          image: rpizziol/acmeair-bookingservice-springboot:0.14
          imagePullPolicy: Always
          resizePolicy:
            - resourceName: cpu
              restartPolicy: NotRequired
          resources:
            requests:
              memory: "1Gi"
              cpu: "1000m" # Recommended to use the m notation
            limits:
              memory: "1Gi"
              cpu: "1000m" # 16000m
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          ports:
            - containerPort: 80
